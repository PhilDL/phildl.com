---
locale: en
translationKey: ts-ghost
title: "ts-ghost"
description: "Type-safe TypeScript toolkit for Ghost CMS with validated API clients, schema-safe data handling, and a CLI for content export and automation."
kind: open-source
status: active
featured: true
role: Creator and maintainer
technologies:
  - TypeScript
  - Zod
  - Ghost Content API
  - Ghost Admin API
  - CLI tooling
highlights:
  - Type-safe wrappers around Ghost Content and Admin APIs for safer integrations.
  - Runtime validation with Zod to detect invalid content payloads early.
  - CLI support for exporting content to Markdown or JSON for migrations and automation.
links:
  live: https://ts-ghost.dev/
---

`ts-ghost` packages the Ghost Content API, the Admin API, and a CLI into a
single TypeScript-first toolkit.

The goal is to make content automation safer and more ergonomic by validating
data at runtime while preserving strong static types across the whole workflow.
