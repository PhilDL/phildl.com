---
locale: en
translationKey: ts-ghost
title: "ts-ghost"
description: "Type-safe tooling for Ghost blogs, with validated API clients and a CLI for content extraction."
kind: open-source
status: active
featured: true
role: Creator and maintainer
technologies:
  - TypeScript
  - Zod
  - Ghost Content API
  - Ghost Admin API
  - CLI tooling
highlights:
  - End-to-end type safety around Ghost APIs and content workflows.
  - Runtime validation to catch invalid data early.
  - CLI support for exporting content into Markdown or JSON.
links:
  live: https://ts-ghost.dev/
---

`ts-ghost` packages the Ghost Content API, the Admin API, and a CLI into a
single TypeScript-first toolkit.

The goal is to make content automation safer and more ergonomic by validating
data at runtime while preserving strong static types across the whole workflow.
